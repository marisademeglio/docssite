{
  "name": "website",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "build": "./node_modules/.bin/cross-env VERSION=$TRAVIS_TAG ./node_modules/.bin/eleventy && ./node_modules/.bin/cross-env WRITE_VERSION='yes' ./node_modules/.bin/cross-env VERSION=$TRAVIS_TAG ./node_modules/.bin/eleventy",
    "serve": "./node_modules/.bin/cross-env VERSION=$TRAVIS_TAG ./node_modules/.bin/eleventy --watch --serve",
    "test": "rm -rf _site/ && cp test/versions-1.json src/_data/versions.json && export TRAVIS_TAG=0.1.0 && npm run build && cp test/versions-2.json src/_data/versions.json && export TRAVIS_TAG=0.2.0 && npm run build && cp test/versions-3.json src/_data/versions.json && export TRAVIS_TAG=0.3.0 && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@11ty/eleventy": "^0.10.0",
    "cross-env": "^7.0.0",
    "luxon": "^1.22.0",
    "markdown-it": "^10.0.0",
    "markdown-it-anchor": "^5.2.5",
    "nunjucks": "^3.2.0",
    "semver": "^7.1.2"
  }
}
